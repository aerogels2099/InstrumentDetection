# Instrument Detection

A web application for detecting musical instruments in audio files.
This project uses a FastAPI backend for audio processing and a React frontend for user interaction. The core audio model is a CRNN (Convolutional Recurrent Neural Network).

## Acknowledgments

This project uses a dynamic background inspired by the
[AmbientCanvasBackgrounds](https://github.com/crnacura/AmbientCanvasBackgrounds)
project by *crnacura*.
